diff --git a/build.gradle b/build.gradle
index 342a85c96..8bd37e7c4 100644
--- a/Mindustry/build.gradle
+++ b/Mindustry/build.gradle
@@ -135,6 +135,7 @@ allprojects{
                 props["build"] = buildid
 
                 props.store(pfile.newWriter(), "Autogenerated file. Do not modify.")
+                pfile.text = pfile.readLines()[2..-1].join('\n') // remove timestamp by deleting the first two lines
             }
         }
 
